<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <data>

        <record id="view_account_payment_form" model="ir.ui.view">
            <field name="name">account.payment.view.form.inherit</field>
            <field name="model">account.payment</field>
            <field name="inherit_id" ref="account.view_account_payment_form" />
            <field name="arch" type="xml">
                <xpath expr="//field[@name='ref']" position="after">
                    <field name="is_advance" invisible="1" />
                    <field name="advance_account_id" invisible="1" />
                </xpath>
            </field>
        </record>

        <record id="account_advance_payment_action" model="ir.actions.act_window">
            <field name="name">Advance Payment</field>
            <field name="res_model">account.payment</field>
            <field name="view_mode">list,form</field>
            <field name="domain">[('is_advance', '=', True)]</field>
            <field name="context">{
                'default_is_advance': True,
                'is_advance': True,
                'default_payment_type': 'inbound',
                'default_partner_type': 'customer',
                'search_default_inbound_filter': 1,
                'default_move_journal_types': ('bank', 'cash'),
                }</field>
        </record>

        <menuitem
            id="account_advance_payment_menu"
            name="Advance Payment"
            action="account_advance_payment_action"
            parent="account.menu_finance_receivables"
            sequence="11" />

    </data>

</odoo>